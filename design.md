# POLYARENA — 실시간 멀티플레이어 탱크 아레나

> 탑다운 2D 아레나 슈터. 도형을 부숴 경험치를 얻고, 레벨업으로 스탯과 탱크 클래스를 성장시켜
> 다른 플레이어와 싸운다. (diep.io 장르 참고, 에셋·이름·밸런스·코드는 전부 자체 제작)

---

## 1. 목표

| 항목 | 내용 |
|------|------|
| 장르 | 실시간 멀티플레이어 탑다운 슈터 (.io) |
| 플랫폼 | 웹 브라우저 (데스크톱 + 모바일 터치) |
| 배포 | Cloudflare Workers + Durable Objects |
| 동시접속 | 룸당 최대 40명 (룸 샤딩으로 확장) |
| 목표 지연 | 국내 기준 왕복 60ms 이하, 30Hz 틱 |

---

## 2. 아키텍처

```
브라우저 (클라이언트)                   Cloudflare
┌──────────────────────┐              ┌─────────────────────────────┐
│ index.html           │  HTTP        │ Worker (src/index.js)       │
│ js/main.js  게임루프  │ ───────────▶ │  · 정적 에셋 서빙 (ASSETS)    │
│ js/render.js 렌더링   │              │  · /ws → Durable Object 라우팅│
│ js/input.js  입력     │              └──────────────┬──────────────┘
│ js/net.js    통신     │  WebSocket                  │
│ js/ui.js     HUD      │ ◀══════════════════════════▶ ▼
└──────────────────────┘   바이너리    ┌─────────────────────────────┐
                                       │ Durable Object: GameRoom     │
        ┌──────────────────────────────│  · 30Hz 권위 시뮬레이션       │
        │  public/shared/  (공용 모듈)  │  · 충돌/데미지/레벨링         │
        │   config.js  게임 상수·수식    │  · 시야 컬링 후 스냅샷 전송   │
        │   tanks.js   28종 탱크 정의    │  · 봇 AI로 룸 인원 채우기     │
        │   protocol.js 바이너리 프로토콜 │  └─────────────────────────────┘
        └──────────────────────────────┘
```

**서버 권위(Server-authoritative)** — 클라이언트는 *입력만* 보낸다. 위치·체력·데미지·경험치는
전부 서버가 계산한다. 클라이언트 조작으로는 치트가 불가능하다.

**클라이언트 보정**
- 내 탱크: 클라이언트 예측(prediction) + 서버 값으로 부드럽게 보정(reconciliation)
- 남의 탱크/총알/도형: 100ms 렌더 지연 + 스냅샷 사이 보간(interpolation)

---

## 3. 게임 규칙

### 3.1 성장
| 요소 | 값 |
|------|-----|
| 최대 레벨 | 45 |
| 경험치 곡선 | `1.15 · L^2.05 + 12L` |
| 스탯 포인트 | Lv2~28 매 레벨 +1, 이후 Lv30/33/36/39/42/45 +1 → 총 33 |
| 스탯 최대치 | 종류당 7 (8종 × 7 = 56 > 33 → 빌드 선택 강제) |
| 클래스 승급 | Lv15 / Lv30 / Lv45 |

### 3.2 스탯 8종
체력 재생 · 최대 체력 · 몸통 데미지 · 총알 관통 · 총알 데미지 · 총알 속도 · 재장전 · 이동 속도

### 3.3 탱크 트리 (28종)
```
기본
├── 쌍포 ────┬── 삼연포 ──┬── 산탄포
│            │            └── 삼련포
│            ├── 쌍둥이플랭크 ─┬── 팔방포
│            │                └── 삼중쌍포
│            └── 사방포 ────────── 팔방포
├── 저격수 ──┬── 암살자 ──┬── 레인저
│            │            └── 스토커  (정지 시 은신)
│            └── 사냥꾼 ───── 포식자
├── 기관총 ──┬── 파괴자 ──┬── 섬멸자
│            │            └── 파쇄자
│            ├── 포수 ────┬── 스트리머
│            │            └── 자주포
│            └── 살포기 ────── 폭풍
└── 플랭크 ──┬── 삼각날개 ┬── 부스터   (후방 추진)
             │            └── 전투기
             ├── 사방포
             └── 쌍둥이플랭크
```
탱크는 **데이터**로 정의된다 — 포신 배열(각도/위치/길이/폭/재장전/데미지/반동…)만 있으면
서버 사격 로직과 클라이언트 렌더링이 자동으로 따라온다. 새 클래스 추가 = 배열 한 줄.

### 3.4 중립 도형
| 도형 | 체력 | 경험치 | 비중 |
|------|------|--------|------|
| 사각형 | 12 | 12 | 60% |
| 삼각형 | 32 | 30 | 30% |
| 오각형 | 110 | 140 | 9% |
| 알파 오각형 | 2600 | 3000 | 1% |

아레나에 항상 460개 유지, 파괴되면 시야 밖에서 리스폰.

### 3.5 보스 — 디펜더

아레나에 항상 **1기**가 무작위 위치에 존재한다. 처치되면 45초 뒤 다른 곳에 다시 나타난다.

| 항목 | 값 |
|------|-----|
| 체력 | 14,000 (알파 오각형의 5배) |
| 이동 속도 | 46 u/s — 일반 탱크(380)의 **1/8** |
| 몸통 | 삼각형, 반지름 132 (일반 탱크의 4.4배) |
| 처치 보상 | **30,000 경험치** — 한 방에 Lv37 |

**두 종류의 무기**

1. **트랩 사출기 3문** — 삼각형 세 꼭짓점의 큰 포구.
   트랩은 약 270유닛만 나아가고 멈춘 뒤 **13초간 그 자리에 남는다**(총알 수명의 6.2배).
   본체가 초당 0.22rad 씩 돌기 때문에 트랩이 부채꼴로 깔려 길목이 막힌다.
2. **소형 포탑 3문** — 몸통 위에 얹힌 작은 포구.
   **본체와 따로, 각자 가까운 적을 조준**해 작은 포탄을 빠르게 퍼붓는다.

**구현 방식** — 포탑은 본체와 별개 엔티티로 스냅샷에 실린다.
엔티티 하나에 각도가 하나뿐인 12바이트 포맷을 그대로 두면서 독립 조준을 얻기 위한 선택이다.
본체·포탑·트랩·포탄은 `teamId` 를 공유해 서로를 때리지 않는다.
포탑은 `noCollide` 라 피격 판정이 없고, 데미지는 전부 본체가 받는다.

### 3.6 전투
- 총알은 **관통력(health)** 을 가진다. 명중 시 서로 데미지를 교환하고, 관통력이 남으면 관통한다.
- 트랩은 총알과 같은 경로로 처리되지만 감속해 멈추고 오래 남는다 (`KIND.TRAP`).
- 몸통 충돌은 지속 데미지 + 물리적 밀어내기.
- 반동(recoil)은 실제 물리 임펄스 → 후방 포신 클래스(삼각날개/부스터)는 사격으로 가속한다.

---

## 4. 네트워크 프로토콜 (바이너리)

JSON은 스냅샷당 10KB를 넘어 대역폭이 감당이 안 된다. `DataView` 기반 바이너리를 쓴다.

**클라이언트 → 서버**
| 코드 | 이름 | 페이로드 |
|------|------|----------|
| 1 | INPUT | flags(u8) + 조준각(u16) + seq(u16) — **5바이트** |
| 2 | SPAWN | 닉네임 |
| 3 | STAT | 스탯 인덱스 |
| 4 | CLASS | 클래스 ID |
| 5 | PING | 타임스탬프 |

**서버 → 클라이언트**
| 코드 | 이름 |
|------|------|
| 1 | WELCOME (내 ID, 아레나 크기) |
| 2 | SNAPSHOT (내 상태 + 시야 내 엔티티) |
| 3 | NAMES (ID→닉네임, 로스터 변경 시에만) |
| 4 | LEADERBOARD (0.5초마다 상위 10명) |
| 5 | DEATH |
| 6 | PONG |
| 7 | ERROR |

**엔티티 12바이트 고정**
`kind(u8) id(u16) x(i16) y(i16) angle(u16) radius(u8) hp(u8) color(u8) sub(u8) alpha(u8)`

`kind` 는 0=탱크, 1=총알, 2=도형, 3=트랩. `sub` 는 탱크면 클래스 ID, 도형이면 도형 종류.

시야 컬링(공간 해시 그리드) 후 최대 220개 → 스냅샷 ≈ 1.5KB, 30Hz → **약 45KB/s**.

---

## 5. Durable Object 설계

- `idFromName(roomName)` 으로 룸당 DO 인스턴스 1개 → 룸 내 모든 상태가 한 곳에 있어 락이 필요 없다.
- WebSocket은 **비-하이버네이션** 모드(`ws.accept()`). 연결이 살아있는 동안 DO가 메모리에 상주하므로
  `setInterval` 게임 루프가 유지된다.
- 마지막 플레이어가 나가면 루프를 정지 → DO가 유휴 상태로 내려가 비용 0.
- 무료 플랜 대응: 마이그레이션에 `new_sqlite_classes` 사용.

---

## 6. 파일 구조

```
app/
├── wrangler.jsonc            Cloudflare 설정
├── package.json
├── src/                      ← Worker 번들 (서버)
│   ├── index.js              라우팅 + 에셋
│   ├── room.js               GameRoom Durable Object
│   └── game/
│       ├── world.js          시뮬레이션 루프
│       ├── grid.js           공간 해시
│       ├── entities.js       Bullet / Shape
│       ├── player.js         플레이어 탱크
│       ├── firing.js         포신 발사 (플레이어·보스 공용)
│       ├── boss.js           디펜더 보스 + 포탑
│       └── bot.js            봇 AI
└── public/                   ← 정적 에셋 (클라이언트)
    ├── index.html
    ├── style.css
    ├── shared/               서버·클라이언트 공용
    │   ├── config.js
    │   ├── tanks.js
    │   └── protocol.js
    └── js/
        ├── main.js  net.js  input.js  render.js  ui.js
```

`public/shared/` 는 브라우저가 ES 모듈로 직접 로드하고, 서버는 `../public/shared/…` 로 import
하여 wrangler(esbuild)가 번들에 포함시킨다 → **상수·수식·탱크 정의가 절대 어긋나지 않는다.**

---

## 7. 배포

```bash
cd app
npm install
npx wrangler dev      # 로컬 http://localhost:8787
npx wrangler deploy   # → https://polyarena.<계정>.workers.dev
```

커스텀 도메인은 Cloudflare 대시보드 → Workers → Custom Domains 에서 연결.

---

## 8. 검증 기준 (Check)

- [ ] 두 브라우저 탭에서 서로의 탱크·총알이 보이고 데미지가 동기화된다
- [ ] 30Hz 틱이 유지되고 스냅샷이 2KB를 넘지 않는다
- [ ] 레벨업 → 스탯 포인트 → 클래스 승급이 서버에서만 결정된다
- [ ] 봇이 룸을 채워 혼자 접속해도 게임이 성립한다
- [ ] 보스가 1기 존재하고, 트랩을 깔고, 포탑 3문이 따로 조준한다
- [ ] 보스를 처치하면 30,000 경험치를 받는다
- [ ] 모바일 터치(듀얼 스틱)로 이동·조준·사격이 가능하다
- [ ] `wrangler deploy` 후 공개 URL에서 정상 동작한다
